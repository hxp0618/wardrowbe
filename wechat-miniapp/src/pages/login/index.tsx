import { Text, View } from '@tarojs/components'

export default function LoginPage() {
  return (
    <View
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#ffffff',
      }}
    >
      <Text style={{ fontSize: '18px', color: '#111111' }}>Wardrowbe Login Debug</Text>
    </View>
  )
}
